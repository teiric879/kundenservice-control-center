{
  "name": "eregio-control-center",
  "version": "1.0.0",
  "scripts": {
    "build:fn": "esbuild backend/serverless-entry.js --bundle --platform=node --target=node20 --outfile=api/index.js",
    "vercel-build": "echo \"api/index.js is pre-bundled — no build needed\""
  },
  "dependencies": {
    "@fastify/compress": "^7.0.3",
    "@fastify/cors": "^9.0.1",
    "@fastify/helmet": "^11.1.1",
    "@fastify/rate-limit": "^9.1.0",
    "@libsql/client": "^0.17.4",
    "@vercel/functions": "^2.2.13",
    "fastify": "^4.28.0",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  },
  "engines": {
    "node": ">=20"
  }
}
